using DG.Tweening;
using NUnit.Framework;
using System.Collections.Generic;
using UnityEngine;

public class GoBack : MonoBehaviour
{
    //public List<DOTweenAnimation> backUIElements = new List<DOTweenAnimation>();
   // public List<DOTweenAnimation> hideThese = new List<DOTweenAnimation>();
    /*public void GoBackMenu()
    {
        for (int i = 0; i < backUIElements.Count; i++)
        {
            if (backUIElements[i].gameObject.activeSelf == false) backUIElements[i].gameObject.SetActive(true);
            backUIElements[i].DOPlay();
        }
        for(int x = 0; x < hideThese.Count; x++)
        {
            hideThese[x].DOKill();
            if (hideThese[x].gameObject.activeSelf == true)
            {
                hideThese[x].gameObject.SetActive(false);
            }
        }
    }*/
}
